{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug test_kernels (Build first)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/test_kernels",
      "preLaunchTask": "Build (make)",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "setupCommands": [
        { "description": "Enable pretty-printing", "text": "-enable-pretty-printing", "ignoreFailures": true }
      ]
    }
  ]
}
